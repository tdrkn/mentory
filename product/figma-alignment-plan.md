# План приведения реализации к Figma-прототипу

Дата: 2026-05-20.
Источник прототипа: `Figma basics.pdf` (72 экрана).
Базовая ревизия: `e75ae77` (после fix booking flow / verified badge / sessions race / RUB currency).

Этот документ описывает прагматичный порядок работ. Каждая итерация — отдельный PR/коммит c проверкой в Chrome MCP против ключевых экранов прототипа.

---

## Статус после QA 2026-05-21

Последние приложенные PNG-макеты уточнили часть решений из исходного PDF. Главное уточнение: публичный профиль ментора должен показывать справа **два отдельных блока** `Планы подписки` и `Разовые сессии и услуги`, а не старый переключатель `Сессия/Подписка`.

Закрыто и проверено в браузере:

- `/profile/edit`: блоки `Основная информация`, `Фото профиля`, `Карьера`, `Навыки`, `Хобби`, `Достижения`; `Сертификаты` больше не используется как название блока.
- `/mentors/:id`: профиль ментора с отдельными правыми блоками `Планы подписки` и `Разовые сессии и услуги`.
- `/schedule/calendar`: weekly calendar без горизонтального overflow на desktop/mobile.
- `/admin/login`, `/admin`, `/admin/trust#verification|support|database`: вход, dashboard, hash-вкладки, mobile overflow и русские labels финансов.

Не закрыто:

- Новые страницы заявок `/requests/:id/...` и полноценная причина reject.
- Subscription checkout / approve-then-pay.
- UI обработки ready payouts.
- Демо-данные профилей: часть seed/runtime менторов пока без фото, навыков, хобби и достижений.

---

## Принципы выбора порядка

1. **Снять регрессии**, ломающие core-флоу (запись на сессию, отзывы, ссылка на встречу).
2. **Привести state machine к Figma**, чтобы фронт и API говорили на одном языке.
3. **UX-полировка** (табы, формы, лендинг, фильтры) последовательно по разделам.
4. **Админка и календарь** — крупные блоки в самом конце, после стабилизации основного флоу.

Каждая итерация = 1 PR, чтобы было легко ревьюить и откатывать.

---

## Итерация 1 — Регистрация и вход (1 PR)

**Цель:** привести `/login` и `/register` к Figma.

- [ ] `/register`: разделить `fullName` на `firstName` + `lastName` (frontend), отправлять оба поля в `POST /auth/register`. На сервере собрать `fullName = firstName + ' ' + lastName`, сохранить дополнительно `firstName`/`lastName` в БД.
  - Миграция: `users.first_name`, `users.last_name` (nullable). Бэкап-fill из `fullName.split(' ', 1)`.
- [ ] `/register`: карточки роли (Найти ментора / Стать ментором) с описаниями вместо radio.
- [ ] `/login`: убрать username, оставить email-only.
- [ ] При логине сервер всё ещё принимает username для обратной совместимости — фронт перестаёт показывать.

**Тест:** регистрация через UI, проверка фамилии/имени в БД, login по email, login по username (через curl) всё ещё работает.

---

## Итерация 2 — Публичный профиль ментора: правые блоки планов и услуг (закрыто 2026-05-21)

**Цель:** привести `/mentors/:id` к последнему PNG-макету просмотра профиля.

- [x] Sidebar показывает отдельный блок `Планы подписки`.
- [x] Ниже отдельный блок `Разовые сессии и услуги`.
- [x] Карточки selectable, выбранная подсвечивается.
- [x] Хинт `Ближайший свободный слот: <дата>` сохранен для разовых услуг.
- [x] CTA `Забронировать` ведет на `/booking/new?serviceId=...`.
- [x] CTA `Отправить запрос` ведет на `/subscriptions/new?planId=...`.

**Тест:** Playwright/browser smoke на `/mentors/:id` desktop/mobile: оба блока видны, старого текста `Сессия\nПодписка` нет, horizontal overflow = 0.

---

## Итерация 3 — Экран «Заявка на сессию» (новая страница) + state machine pay-first (2 PR)

**Цель:** реализовать full session-request screen и pay-first flow.

### 3.1 Frontend (`/booking/new`)
- [ ] Новая страница: `/booking/new?serviceId=&mentorId=`.
  - Контактная информация: Имя/Фамилия/Email (prefilled, editable).
  - Мотивационное письмо (опционально, single textarea).
  - Выбор временного слота — горизонтальный список карточек, навигация ←/→ страницами по 5 шт.
  - Sticky карточка справа: услуга, mentor name, Стоимость, Длительность, Дата консультации.
  - Кнопка `Оплатить` (вместо текущего `/checkout`).

### 3.2 Backend
- [ ] `POST /api/booking/hold` принимает `requestMotivation` (без `requestGoal`), создаёт session со статусом `requested`.
- [ ] После создания → создаётся `Payment` в state `pending`, mentee redirected на checkout.
- [ ] После успешного payment → session.status = `paid_pending_approval` (новый статус).
- [ ] Ментор Confirm → `booked`. Ментор Reject → refund + session.status `rejected`.
- [ ] Старая модалка booking удаляется.

**Тест:** mentee submit, видит в Управление заявками → Одобренные после approve ментора. Также видит в Мои сессии → Предстоящие после approve.

---

## Итерация 4 — Экран «Заявка на подписку» + approve-first flow (1 PR)

**Цель:** дать подпискам корректный двухшаговый approve-then-pay flow.

- [ ] Новая страница `/subscriptions/new?planId=`.
  - Имя/Фамилия/Email (prefilled).
  - **Цель** (required, single line).
  - **Мотивационное письмо** (optional, textarea).
  - Кнопка `Подать заявку` (не Оплатить).
- [ ] State machine подписки:
  - `pending` (после submit, mentor approval required).
  - `approved_pending_payment` (после mentor approve).
  - `active` (после payment).
  - `rejected` / `canceled`.
- [ ] Эндпоинт `POST /api/subscriptions/:id/pay` для оплаты after approval.
- [ ] У mentee Управление заявками → Подписки → Одобренные показывается CTA «Оплатить заявку», ведёт на детали + Оплатить.

**Тест:** subscription submit → mentor approve → mentee видит «Оплатить заявку» → оплата → подписка активна.

---

## Итерация 5 — «Управление заявками» переработка (1 PR)

**Цель:** привести структуру табов к Figma.

- [ ] Mentor `/admin/requests` или `/dashboard/requests`:
  - Top tabs: `Одиночные сессии` / `Подписки`.
  - Sub-tabs:
    - Одиночные сессии: `Оплаченные / Отклонённые / Все` (без В ожидании потому что pay-first, тёрминальный statuses).
    - Подписки: `В ожидании / Одобренные / Оплаченные / Отклонённые / Все`.
  - Карточка заявки: ФИО, услуга/подписка, дата, badge статуса, кнопки «Профиль менти» + «Чат с менти» + «Посмотреть заявку».
- [ ] Mentee `/requests` или `/dashboard/my-requests`:
  - Top tabs: `Одиночные сессии` / `Подписки`.
  - Sub-tabs:
    - Одиночные сессии: `Одобренные / Отклонённые / Все`.
    - Подписки: `В ожидании / Одобренные / Отклонённые / Все`.
  - Карточка: профиль ментора + кнопки «Профиль ментора» + «Чат с ментором» + «Оплатить заявку» / «Посмотреть заявку».

**Тест:** full path mentee→mentor visible в обеих сторонах.

---

## Итерация 6 — Экран «Заявка на сессию» / «Заявка на подписку» для ментора (1 PR)

**Цель:** окно approve/reject с комментарием.

- [ ] `/requests/:id/session`:
  - Профиль менти с фото + email + ФИО + «Профиль менти» кнопка.
  - Карточка `Мотивационное письмо`.
  - Карточка `Сессия`: услуга, стоимость, продолжительность, дата консультации.
  - Поле `Комментарий для менти (опционально)`.
  - Кнопки `Принять` (если ожидает) / `Отклонить`.
- [ ] `/requests/:id/subscription`:
  - Те же блоки, но Подписка card + `Одобрить` / `Отклонить`.
- [ ] API: `PATCH /sessions/:id/confirm` принимает `mentorComment`. `PATCH /sessions/:id/reject` принимает `mentorComment`. Аналогично для subscriptions.

**Тест:** ментор reject с комментарием → mentee видит комментарий в детали заявки.

---

## Итерация 7 — Видеосвязь как внешняя ссылка (1 PR)

**Цель:** убрать `video_rooms` placeholder, перейти на user-pasted external link.

- [ ] Миграция Prisma: добавить `sessions.video_link` (nullable String). Можно мигрировать данные из старых `video_rooms` если есть.
- [ ] `PATCH /sessions/:id` (mentor only) принимает `videoLink`.
- [ ] Mentor session detail (`/sessions/:id`) показывает input «Прикрепить ссылку на сессию», сохраняет на blur.
- [ ] Mentee session detail показывает ссылку как readonly + clickable (если есть).
- [ ] Старый `video_rooms` endpoint depreciated (оставить шим на квартал).

**Тест:** mentor вставляет URL, mentee видит и кликает.

---

## Итерация 8 — Оценить ментора (отдельный экран) (1 PR)

**Цель:** отдельная страница `/sessions/:id/review` со звёздами.

- [ ] Новая route `/sessions/:id/review`.
- [ ] Карточка прошедшей сессии (фото ментора + name + «Прошедшая сессия: <название>»).
- [ ] 5 интерактивных звёзд + textarea + кнопка «Отправить отзыв».
- [ ] После submit → redirect на `/sessions` с successful banner.
- [ ] Удалить inline review из `/sessions/:id`.

**Тест:** mentee оставляет отзыв, mentor видит его в публичном профиле.

---

## Итерация 9 — Мои сессии: переработка (1 PR)

**Цель:** табы и блок «Свободные временные слоты» для ментора.

- [ ] Tabs: `Предстоящие / Прошедшие / На согласовании / Все`.
- [ ] Карточка сессии минимальная: имя, услуга, дата/время, кнопки «Чат с менти/ментором» + «Подробная информация».
- [ ] У ментора снизу секция `Свободные временные слоты` — пагинированный список свободных слотов с кнопкой «Забронировать» (открывает диалог выбора менти для прямого назначения).

**Тест:** на странице видно сегментирование.

---

## Итерация 10 — Финансы: 4 KPI + история выплат таблица (1 PR)

**Цель:** mentor `/earnings` приведён к Figma.

- [ ] KPI cards: Общий заработок / Доступно к выводу / Проведённые сессии / Рейтинг.
  - Эндпоинт `GET /payouts/balance` расширить: вернуть `totalEarned`, `available`, `sessionsCount`, `ratingAvg`.
- [ ] История выплат — таблица: Менти / Сессия / Дата выплаты / Статус / Сумма.
- [ ] Вывод средств: список methods masked + Удалить + Добавить способ + большая кнопка `Вывести средства`.
- [ ] Валюта RUB.

**Тест:** mentor с историей сессий видит реальные числа.

---

## Итерация 11 — Mentee profile editor: вернуть Карьера + Цели (1 PR)

**Цель:** откатить мой over-eager «hide for mentee» и привести к Figma.

- [ ] Mentee editor показывает: Карьера (Образование, Должность, Место работы, Сфера деятельности — НЕ обязательные для менти), Навыки, Хобби, Достижения.
- [ ] Новая боковая карточка `Добавить цели` — input + список добавленных goals + удаление.
- [ ] PATCH `/profile/mentee` принимает `goals` массив; сохраняем в БД.
- [ ] Профиль менти (просмотр) показывает Цели в highlighted box.
- [ ] Обновить страницу `/mentees/:id` (mentor view) под этот же layout.

**Тест:** mentee Иван Сидоров заполняет цели, mentor в чате видит их в его профиле.

---

## Итерация 12 — Mentor profile editor: Имя/Фамилия раздельно + edit/delete для услуг и планов (1 PR)

- [ ] Имя/Фамилия раздельные.
- [ ] Услуги: rendered как полноценные карточки с edit/delete.
- [ ] Подписки: те же карточки edit/delete.
- [ ] Поле «Должность» — НЕ required для ментора.

---

## Итерация 13 — Каталог менторов: новая шапка + фильтры pills (1 PR)

- [ ] H1 «Найдите идеального ментора» + подзаголовок счётчика.
- [ ] Filter pills (expandable): Навыки / Минимальная цена / Максимальная цена / Сфера деятельности / Рейтинг.
- [ ] Карточка: фото, ФИО, «Эксперт»/«Ментор», rating + sessions, цена «От N руб» + кнопка «Профиль >».
- [ ] Валюта: «руб» (без RUB суффикса) для каталога.

---

## Итерация 14 — Лендинг / Главная (1 PR)

- [ ] Hero: «Находите экспертов и растите быстрее вместе с Mentory» + правая карточка стат «Активных менторов 750+».
- [ ] 3 stat-cards: проведённых сессий / средний рейтинг / время до первой консультации.
- [ ] 3 feature cards.
- [ ] «Как это работает» — 3 шага.
- [ ] «Истории успеха» — 3 цитаты.
- [ ] CTA + Footer 3 columns.

---

## Итерация 15 — Управление слотами: переработка (1 PR)

- [ ] Левая панель: список добавленных слотов (день+время) с Удалить, форма создания.
- [ ] Правая панель: Генерация слотов (С / По / Длительность).
- [ ] Кнопка `Календарь сессий` на главную мента → новая страница `/schedule/calendar`.

---

## Итерация 16 — Календарь сессий (большой блок, можно 2 PR) 

- [ ] `/schedule/calendar`: weekly view с временными ячейками, занятые сессии показываются с менти+услуга, навигация по неделям.
- [ ] Кнопка `Добавить новые слоты` справа сверху.

---

## Итерация 17 — Админка (большой блок, в фоновом backlog)

- [ ] Login /admin/login с 2FA-кодом.
- [ ] Главный экран: 3 cards + counters + Требуемые действия объединённый список.
- [ ] Подразделы: Верификация / Поддержка / База данных.

---

## Что НЕ делать в рамках этого плана

- Не переписывать AdminJS — оставить как fallback. Целевой админ-UI — отдельный путь.
- Не вводить per-mentor URL `PLATFORM/####_(№)` из NFR-листа — это backlog NFR.
- Не делать push-уведомления — пока остаётся только email и in-app.

---

## Открытые вопросы

1. **Mentee Финансы**: на page 56 Figma в навбаре менти есть «Финансы», но скриншот самой страницы менти не виден. Что показывать — историю платежей? Активные подписки?
2. **Подписочные сессии**: после «На согласовании → Согласовать встречу» mentee, как session получает финальный slot? Через mentor proposes, mentee accepts?
3. **Username**: оставлять или окончательно убрать? Сейчас в коде используется для login. Решить: deprecate или нет.
4. **Pay-first vs approve-first для сессий**: page 0 показывает «В ожидании» для одиночной сессии у ментора, но page 54 имеет CTA «Оплатить». Уточнить — paid → goes to В ожидании? Или figma mock inconsistent.

---

## Резюме

17 итераций ~~~ 18 PR при разделении больших блоков. По приоритету: 1-7 — critical core, 8-12 — UX полировка, 13-17 — масштабные доработки. Estimated effort: 1.5–2 sprints для itr 1-7 (если 1 engineer полный day), полный пакет — квартал.
